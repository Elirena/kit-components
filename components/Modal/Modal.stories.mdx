import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import * as stories from './Modal.stories.tsx';
import Modal from '.';
import { ThemeProvider } from 'styled-components';
import { author24 } from '../../theme';

<Meta title="COMPONENTS|Modal" component={Modal} />

<ThemeProvider theme={author24}>
    <Preview withSource="close" mdxSource={`
        <Modal label="Lorem Ipsum">
            Hover cursor over me
        </Modal>
        <Modal
            size="default"
        >
            Or me
        </Modal>
    `}>
        <div style={{
            display: 'grid',
            placeItems: 'center',
            height: '100px',
            font: `${({ theme }) => theme.typography.content_16px.font}`,
        }}>
            <Modal label="Lorem Ipsum">
                <stories.Info>Hover cursor over me</stories.Info>
            </Modal>
            <Modal
               title = "Modal Title"
               size = 'default'
               visible = "true"
               closeable = 'true'
               centred = 'false'
               children = 'Modal text content'
            >
                <stories.Info>Or me</stories.Info>
            </Modal>
        </div>
    </Preview>
</ThemeProvider>

# Props

<Props of={Modal} />

<div style={{display: 'none'}}>

<Story name="Modal">
    {stories.modal()}
</Story>

</div>
